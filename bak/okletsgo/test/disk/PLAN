TEST PLAN for DISK: (by wade)
1.

Huge data for Performance

case 1 PLAN:
1).Generate 100 {___,length};
2).disk_alloc, fill in {ptr, length};
3).disk_free i%2==0, in {ptr, length};
4).{ptr,length} --> bitmap
5).Check 20:
   Generate {___,length};
   disk_alloc, fill in {ptr, length};
   check if invvalid in [bitmap].
   fill in [bitmap].

